/**
 * Fyndiq Products plugin config
 * Skeleton connector: settings + test + export/delete endpoints + channel map updates.
 */
module.exports = {
  name: 'fyndiq-products',
  routeBase: '/api/fyndiq-products',
  requiredRole: 'user',
  description: 'Fyndiq channel: settings + product export/delete (skeleton).',
};

