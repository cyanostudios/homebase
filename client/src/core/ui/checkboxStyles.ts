/** Round checkbox — secondary when idle, primary when checked (matches RoundIconLabelButton). */
export const CHECKBOX_CLASS =
  'hb-checkbox shrink-0 cursor-pointer align-middle disabled:cursor-not-allowed';

/** Compact size for list rows and dense forms. */
export const CHECKBOX_SM_CLASS = 'h-4 w-4';

/** Default touch-friendly size. */
export const CHECKBOX_MD_CLASS = 'h-5 w-5';
