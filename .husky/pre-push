#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npm run check
npm test -- --passWithNoTests
# Prod UI chunk DAG — fails on vendor-shared ↔ app-shell/plugin cycles (white screen TDZ).
npm run build:ui
