/**
 * TEMPLATE — copy this into a real plugin folder, then:
 * 1) Set `name` to plural-kebab (e.g., 'my-items' or 'custom-data')
 * 2) Set `routeBase` to `/api/<name>` (must match `name`)
 * 3) Keep/adjust `requiredRole` as needed
 * 4) Update `description`
 */
module.exports = {
  name: 'rename-me', // TODO: plural-kebab, e.g. 'my-items'
  routeBase: '/api/rename-me', // TODO: must match `name`
  requiredRole: 'user',
  description: 'Template plugin — replace name/routeBase/description.',
};
